const { EmbedBuilder } = require('discord.js')
const { client } = require('../main')

module.exports = {
	name: 'invite',
	description: 'Invite someone to the guild.',
	async execute(message, args) {

	},
};